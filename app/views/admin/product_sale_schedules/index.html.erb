<div class="container"><table id="schedules-table", data-source="<%= admin_product_product_sale_schedules_path(@product, format: :json) %>">
    <div style="margin-bottom: 16px">
      <%= link_to "返回", admin_products_path, class: 'btn btn-default' %>
      <%= link_to "添加排期", new_admin_product_product_sale_schedule_path(@product), class: 'btn btn-primary' %>
    </div>
    <thead>
      <tr>
        <th>闪购开始</th>
        <th>闪购结束</th>
        <th>预告开始</th>
        <th>预告结束</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>
